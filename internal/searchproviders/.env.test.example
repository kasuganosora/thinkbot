# ThinkBot searchproviders 集成测试凭据（本文件会被 gitignore，勿提交）
# 复制为 .env.test 后填入真实值：
#   cp .env.test.example .env.test
#
# 运行：
#   go test -v -run TestIntegration ./internal/searchproviders/ -timeout 60s

# Brave Search API Key（Dashboard → API Keys）
# https://api-dashboard.search.brave.com/
THINKBOT_TEST_BRAVE_API_KEY=<your-brave-api-key>
