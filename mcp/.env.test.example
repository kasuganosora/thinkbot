# MCP 集成测试凭据模板
#
# 用法：
#   cp mcp/.env.test.example mcp/.env.test
#   # 填入下面的真实凭据（.env.test 已被 .gitignore 排除，不会进仓库）
#   go test -v -run TestIntegration ./mcp/ -timeout 180s
#
# 未配置 MCP_INTEG_AUTH_TOKEN 时，相关用例会自动 Skip，
# 因此 `go test ./...` 在没有凭据的机器上依然干净通过。
#
# 优先级：进程环境变量 > mcp/.env.test > 仓库根 .env > 代码内默认值。
# 注意：请勿把真实凭据写回 *.go 或本 example 文件。

# ---- MCP 服务器 ----
# 必填：MCP 服务器鉴权头（含 "Bearer " 前缀）
MCP_INTEG_AUTH_TOKEN=Bearer <your-mcp-token>

# 可选：覆盖默认的服务器名与端点
# MCP_INTEG_SERVER_NAME=web-search-prime
# MCP_INTEG_URL=https://open.bigmodel.cn/api/mcp/web_search_prime/mcp

# ---- LLM API（仅 LLM 编排相关用例需要）----
# 必填（否则 LLM 相关用例 Skip）
MCP_INTEG_LLM_API_KEY=<your-llm-api-key>

# 可选：覆盖默认端点与模型
# MCP_INTEG_LLM_BASE_URL=https://open.bigmodel.cn/api/coding/paas
# MCP_INTEG_LLM_CHAT_PATH=/v4/chat/completions
# MCP_INTEG_LLM_MODEL=glm-5.2
